import axios from 'axios';

export const analyticsService = {
  getDashboardStats: async () => {
    // const response = await axios.get('/api/analytics/dashboard');
    // return response.data;
  },
  
  exportJsonl: () => {
    // window.open('/api/analytics/export/jsonl', '_blank');
  }
};
